import { FilterEtiquetasPipe } from './filter-etiquetas.pipe';

describe('FilterEtiquetasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEtiquetasPipe();
    expect(pipe).toBeTruthy();
  });
});
